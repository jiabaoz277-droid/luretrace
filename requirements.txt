# 锁定到已测试版本，保证各机器安装结果一致（如需最新版可改回 >=）
anthropic==0.123.0
python-dotenv==1.2.3
pyyaml==6.0.3
# 路迹 luji 第 1 阶段（FastAPI 服务 + 验收界面 + 测试）
fastapi==0.141.1
uvicorn[standard]==0.52.4
pytest==9.1.1
httpx==0.28.1